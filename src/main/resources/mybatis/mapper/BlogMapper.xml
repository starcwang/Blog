<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.star.blog.dao.BlogDao">


    <resultMap id="BaseResultMap" type="com.star.blog.model.pojo.BlogModel">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="author" property="author" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="key_words" property="keyWords" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        select id, title, content, author, key_words from blog where id=#{id};
    </select>

    <insert id="insertBlogModelReturnId" parameterType="com.star.blog.model.pojo.BlogModel">
       insert into blog (title, author, content, key_words) value (#{title}, #{author}, #{content}, #{keyWords});
    </insert>

    <select id="selectAll" resultMap="BaseResultMap">
        select id, title, content, author, key_words from blog;
    </select>

</mapper>